# Secrets — never commit
.env
.env.local
.env.*.local

# Your own subscriber settings — never commit (this repo is public)
/subscribers.local.yml

# Generated digest output
/digest.html
/digest.preview.html
/digest-subscriber-*.html

# Legacy: accounts now live in subscribers.local.yml / subscriber gists
/accounts.txt

# Scratch directory — smoke test, one-off helpers, account-list dumps
/tmp/

# Ruby / Bundler
/vendor/bundle/
/.bundle/
*.gem

# Tool state
/.ruby-lsp/
/.claude/

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp
*.swo
